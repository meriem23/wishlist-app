// wishlists.forEach((wish) => {
//     console.log(wish.wishlist); get the wishlist name
//   })

// wishlists.forEach((wish) => {
//     console.log(wish.product); // array of products
//   })

// wishlists.forEach((wish) => {
//     console.log(wish.product.map((el) => el.Name)); //array with products name
//   })
{
  /* <button
onClick={() => {
  wishlists.forEach((wish) => {
    console.log(wish.product.map((prod) => <p>prod.Name</p>));
  });
}}
>
click
</button> */
}
